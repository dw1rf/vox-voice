# === Vox — голосовой ИИ-ассистент ===
# Устанавливается через setup.bat (не вручную).

# Распознавание речи (автоматически тянет CUDA-библиотеки nvidia если есть GPU)
faster-whisper>=1.0.0
sounddevice>=0.4.6

# TTS — нейронный голос Microsoft Edge
edge-tts>=7.0.0
pygame>=2.5.0

# UI и трей
pywebview>=5.0.0
pystray>=0.19.0
pillow>=10.0.0

# Горячие клавиши и буфер обмена
keyboard>=0.13.5
pyperclip>=1.8.0

# Нечёткое совпадение команд
rapidfuzz>=3.0.0

# Асинхронность для edge-tts
aiohttp>=3.8.0
